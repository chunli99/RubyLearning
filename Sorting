# Has the ability to sort numbers or alphabetize arrays.
# todo: add input from user

def alphabetize (arr, rev=false)
    arr.sort!
    if rev
        arr.reverse!
    else
        arr
    end
end

numbers = [800, 588, 2300, 3500]

puts alphabetize(numbers)
